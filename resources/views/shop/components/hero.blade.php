@if($heroBanners->isNotEmpty() || $heroSideBanners->isNotEmpty())
<div class="container-fluid carousel bg-light px-0"><div class="row g-0 justify-content-end">
@if($heroBanners->isNotEmpty())<div class="col-12 col-lg-7 col-xl-9" style="direction:ltr"><div class="header-carousel owl-carousel bg-light py-5">@foreach($heroBanners as $banner)@php($translation=$banner->translations->first())<div class="row g-0 header-carousel-item align-items-center"><div class="col-xl-6 carousel-img"><img src="{{ $banner->image_url }}" class="img-fluid w-100" alt="{{ $translation->image_alt?:$translation->title }}"></div><div class="col-xl-6 carousel-content p-4">@if($translation->eyebrow)<h4 class="text-uppercase fw-bold mb-4">{{ $translation->eyebrow }}</h4>@endif<h1 class="display-3 mb-4">{{ $translation->title }}</h1>@if($translation->body)<p class="text-dark">{{ $translation->body }}</p>@endif @if($translation->button_label&&$translation->link_url)<a class="btn btn-primary rounded-pill py-3 px-5" href="{{ $translation->link_url }}" @if(str_starts_with($translation->link_url,'https://'))target="_blank" rel="noopener noreferrer"@endif>{{ $translation->button_label }}</a>@endif</div></div>@endforeach</div></div>@endif
@if($side=$heroSideBanners->first())@php($translation=$side->translations->first())<div class="col-12 col-lg-5 col-xl-3"><div class="carousel-header-banner h-100"><img src="{{ $side->image_url }}" class="img-fluid w-100 h-100" style="object-fit:cover" alt="{{ $translation->image_alt?:$translation->title }}"><div class="carousel-banner"><div class="carousel-banner-content text-center p-4"><h2 class="text-white">{{ $translation->title }}</h2><p class="text-white">{{ $translation->body }}</p></div>@if($translation->button_label&&$translation->link_url)<a class="btn btn-primary rounded-pill py-2 px-4" href="{{ $translation->link_url }}" @if(str_starts_with($translation->link_url,'https://'))target="_blank" rel="noopener noreferrer"@endif>{{ $translation->button_label }}</a>@endif</div></div></div>@endif
</div></div>
@endif
