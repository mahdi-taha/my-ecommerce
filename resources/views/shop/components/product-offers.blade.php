@if($offerBanners->isNotEmpty())<div class="container-fluid py-5"><div class="container"><div class="row g-4">@foreach($offerBanners as $banner)@php($translation=$banner->translations->first())<div class="col-lg-6"><a href="{{ $translation->link_url?:'#' }}" class="d-flex align-items-center justify-content-between border bg-white rounded p-4" @if($translation->link_url&&str_starts_with($translation->link_url,'https://'))target="_blank" rel="noopener noreferrer"@endif><div>@if($translation->eyebrow)<p class="text-muted mb-3">{{ $translation->eyebrow }}</p>@endif<h3 class="text-primary">{{ $translation->title }}</h3>@if($translation->body)<p>{{ $translation->body }}</p>@endif</div><img src="{{ $banner->image_url }}" class="img-fluid" alt="{{ $translation->image_alt?:$translation->title }}"></a></div>@endforeach</div></div></div>@endif
